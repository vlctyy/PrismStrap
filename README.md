A script that attempts to recreate Bloxstrap, so you can use FFlags flawlessly.

```lua
loadstring(game.HttpGet(game, 'https://raw.githubusercontent.com/new-qwertyui/Bloxstrap/main/loader.lua', true))()
```
